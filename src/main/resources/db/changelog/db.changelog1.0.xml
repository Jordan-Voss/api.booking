<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog" xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">
    <changeSet id="1" author="sha">
        <sql>
            CREATE TABLE user (
            id BIGINT NOT NULL AUTO_INCREMENT,
            username VARCHAR(255) NOT NULL,
            password VARCHAR(255) NOT NULL,
            f_name VARCHAR(255) NOT NULL,
            l_name VARCHAR(255) NOT NULL,
            role VARCHAR(255) NOT NULL,
            CONSTRAINT PK_id PRIMARY KEY (id)
            );
        </sql>
        <rollback>
            DROP TABLE user;
        </rollback>
    </changeSet>
    <changeSet id="2" author="sha">
        <sql>
            CREATE TABLE lesson (
            id BIGINT NOT NULL AUTO_INCREMENT,
            day VARCHAR(255) NOT NULL,
            time VARCHAR(255) NOT NULL,
            date VARCHAR(1000),
            CONSTRAINT PK_id PRIMARY KEY (id)
            );
        </sql>
        <rollback>
            DROP TABLE lesson;
        </rollback>
    </changeSet>
    <changeSet id="3" author="sha">
        <sql>
            INSERT INTO lesson (day, time, date) values("Product 1", "19.99",
            "Product is so effective and has many attributes like X, Y, Z. Also, we can say about it...");
            INSERT INTO lesson (day, time, date) values("Product 2", "10.99",
            "Product is so effective and has many attributes like X, Y, Z. Also, we can say about it...");
</sql>
        <rollback>
            TRUNCATE TABLE lesson;
        </rollback>
    </changeSet>
    <changeSet id="4" author="sha">
        <sql>
            CREATE TABLE transaction(
            id BIGINT NOT NULL AUTO_INCREMENT,
            lesson_id BIGINT NOT NULL,
            user_id BIGINT NOT NULL,
            purchase_date DATETIME NOT NULL,
            CONSTRAINT PK_id PRIMARY KEY (id),
            CONSTRAINT fk_tran_lesson FOREIGN KEY (lesson_id) REFERENCES lesson(id) ON DELETE CASCADE ON UPDATE
            CASCADE,
            CONSTRAINT fk_tran_user FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE ON UPDATE
            CASCADE
            );
        </sql>
        <rollback>
            DROP TABLE transaction;
        </rollback>
    </changeSet>
</databaseChangeLog>